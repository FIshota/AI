# PR Plan — 2026-04-23

**作成日**: 2026-04-23
**対象リポジトリ**: `ai-chan` (phase0/rebrand-bench-baseline) + `hinomoto-model` (main)
**背景**: 本日の作業 (SFT continue 起動 / scaled_v3 準備 / eval ツール / CI / docs / archive) を整理して PR 化する。

---

## 方針サマリ

| リポ | ブランチ | PR 分割 | 状態 |
|---|---|---|---|
| ai-chan | `phase0/rebrand-bench-baseline` (既存) | 1 PR にまとめる | ready |
| hinomoto-model | `feature/sft20k-scaled-v3-prep` (新規) | 2 PR に分ける | branch 切替要 |

ルール: **同じ目的・同じレビュー観点のものは 1 PR にまとめる / 別観点は分ける**。

---

## ① ai-chan: 1 PR

### ブランチ
`phase0/rebrand-bench-baseline` (既存。main にまだマージされていない既存変更と混ざるので、本 PR は既存コミット列に追加する形で積む)

### 含める変更

| ファイル | 種別 | 目的 |
|---|---|---|
| `requirements.txt` | M | H11: click 8.3.3 への micro bump (CVE 予防) |
| `tests/test_high_regression.py` | M | H10: FastAPI pin チェックを regex 比較に堅牢化 |
| `docs/ROADMAP.md` | A | 公開用ロードマップ (Phase 0→5+) |
| `docs/security/POLICY_REVIEW_20260423.md` | A | CVE accepted entries の四半期レビュー |
| `docs/security/OUTDATED_AUDIT_20260423.md` | A | 依存監査結果 (40 件トリアージ) |

### コミット構成案 (4 コミット)

1. `test(regression): H10 fastapi pin test to regex-based floor check`
2. `chore(deps): click 8.3.1 → 8.3.3 (no-op micro, CVE 予防ライン維持)`
3. `docs(security): 2026-04-23 quarterly review + outdated audit`
4. `docs(roadmap): public ROADMAP.md for Phase 0→5+`

### PR タイトル / 本文
- **Title**: `chore+docs: 2026-04-23 security audit, roadmap, regression hardening`
- **Summary**
  - H10/H11 regression test harden + click micro bump
  - 公開 ROADMAP を追加
  - CVE 受容レビュー (2 件とも継続) + 依存監査結果
- **Test plan**
  - [x] `pytest tests/test_high_regression.py -v` all pass
  - [x] `git diff requirements.txt` は click 1 行のみ
  - [ ] CI (GitHub Actions) の regression job が green

### レビュー観点
- ROADMAP.md の公開範囲: **Ai (個人用派生)** のコード名を含まないこと → OK (「公式派生 A/B」「内部専用」と抽象化済み)

---

## ② hinomoto-model: 2 PR

### 現状
- branch: `main` (直接作業してしまっている状態)
- 未追跡 / 変更ファイルに **性質の異なる 2 軸** が混在:
  - **軸 A (運用/ドキュメント)**: README 更新、quantize_fp16 fix、RUNBOOK、eval_hinomoto、.github/ CI、scaled_v3.json、sft_dolly_continue.sh
  - **軸 B (学習成果物)**: artifacts/sft_dolly_v1_continue/\* (実行中成果物、**PR に含めない**)

### Step 0 — branch 切替

```bash
cd hinomoto-model
git checkout -b feature/sft20k-scaled-v3-prep
# ↑ main は触らない。これ以降の commit/PR はこのブランチで行う
```

### Step 1 — .gitignore 強化 (PR に含める)

```gitignore
# 学習中のログ (再現に不要、毎回変わる)
artifacts/**/nohup.log
artifacts/**/train.log
artifacts/**/corpus_manifest.json
```

`*.pt` は既に ignored。 **artifacts 下のログは今後も含めない方針** を明示する。

---

### PR #1 (hinomoto): ツーリング + CI + ドキュメント

#### 含める変更

| ファイル | 種別 | 目的 |
|---|---|---|
| `.gitignore` | M | artifacts 配下ログの除外 |
| `.github/workflows/corpus_isolation.yml` | A | P1-5 ガードの CI 化 |
| `README.md` | M | Phase 表記更新 + benchmark snapshot |
| `docs/RUNBOOK_WINDOWS_SCALED.md` | A | Windows GPU 到着後の手順書 |
| `configs/scaled_v3.json` | A | scaled_v3 学習設定 |
| `scripts/quantize_fp16.py` | M | fp16 量子化の細かい fix |

#### コミット構成案 (4 コミット)

1. `chore(git): ignore artifacts/** training logs`
2. `ci(corpus-isolation): GitHub Actions workflow for P1-5 guard`
3. `docs(readme+runbook): Phase 2 snapshot + Windows scaled_v3 runbook`
4. `feat(configs): scaled_v3 (d_model=512 / n_layers=12) config + minor quantize fix`

- **Title**: `feat+docs+ci: scaled_v3 prep, Windows runbook, corpus-isolation CI`
- **レビュー観点**: 実コード (モデル) に触らないので低リスク

---

### PR #2 (hinomoto): SFT continue + evaluation ツール

#### 含める変更

| ファイル | 種別 | 目的 |
|---|---|---|
| `scripts/sft_dolly_continue.sh` | A | step13050→20000 追加 SFT launcher |
| `scripts/eval_hinomoto.py` | A | BLEU + PPL + sample 評価 CLI |

#### コミット構成案 (2 コミット)

1. `feat(sft): continue launcher (13050→20000, lr=3e-6, warmup=20)`
2. `feat(eval): eval_hinomoto BLEU/PPL comparison CLI`

- **Title**: `feat(sft+eval): SFT continue 20k + BLEU/PPL evaluation tool`
- **Depends on**: PR #1 (CI/docs) が先に入ると review がクリーン
- **Test plan**
  - [ ] SFT continue 20000 step 完走 (~PR 開く時点で終わってる想定)
  - [ ] `scripts/eval_hinomoto.py --ckpt ...13050... --compare ...20000...` が両者の BLEU/PPL を出力

#### PR に含めない (**重要**)

- `artifacts/sft_dolly_v1/ckpt_step_*.pt` (約 100MB × 20 個)
- `artifacts/sft_dolly_v1_continue/` 配下すべて
  → `.gitignore` で除外済み or 追加で除外

---

## ③ 作業順序

```
1. ai-chan で 4 commit 積む → push → PR open
2. hinomoto-model: git checkout -b feature/sft20k-scaled-v3-prep
3. .gitignore 編集 + 4 commit (PR #1 分)
4. push → PR #1 open
5. SFT continue 完走を待つ
6. eval_hinomoto.py で before/after 測定 → 結果を docs/ に追記 (PR #2)
7. sft_dolly_continue.sh + eval_hinomoto.py を追加 2 commit
8. push → PR #2 open (PR #1 マージ後 rebase)
```

---

## ④ 除外リスト (今回の PR に**含めない**もの)

| 項目 | 理由 |
|---|---|
| `artifacts/sft_dolly_v1*/*.pt` | バイナリ、git には入れない |
| `artifacts/sft_dolly_v1/train.log` `nohup.log` | 再現不要、.gitignore で除外 |
| `logs/watchdog/watchdog-20260423.log` | 稼働ログ、PR とは独立 (別途 commit 可) |
| `docs/VISION_INTERNAL.md` | 非公開 (既に gitignore 済み) |
| launchd plist / `scripts/daily_security_audit.sh` 関連 | 既に main にある想定 |

---

## ⑤ チェックリスト (PR open 前)

- [ ] `pytest` 全パス (ai-chan / hinomoto 両方)
- [ ] `git log --oneline` が本 plan と一致
- [ ] `.pt` が commit に混入していない (`git diff --cached --stat | grep '\.pt'` が空)
- [ ] ROADMAP.md に **非公開派生モデルの固有名** が出ていない
- [ ] ROADMAP 「Phase 3 遷移条件」の未チェック項目が正確 (BLEU/PPL 数値はまだ未計測)

---

## 参考

- 本日の変更一覧は git status で確認
- 詳細作業ログは `logs/minutes_*` 参照 (古いものは `logs/archive/minutes_2026-04/` に退避済み)
